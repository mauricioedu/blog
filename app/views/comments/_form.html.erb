<%= form_for([@post, @post.comments.build]) do |f| %>
	<%= f.label :nome %><br>
  <%= f.text_field :name %><br>
  <br>
  <%= f.label :comentario %><br>
  <%= f.text_area :body %><br>
  <br>
  <%=  f.submit "Novo Comentario" %>

<% end %>